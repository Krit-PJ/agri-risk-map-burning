# Timeline, comparison table and A4 report update

- Added a linked month timeline at the top of the application.
- Timeline year and month remain synchronized with the existing year/month/day controls.
- Replaced summary cards with a district/subdistrict comparison table.
- The table compares the selected year with the previous year using the same month, day, area and crop filters.
- Percentage change: `(current - previous) / previous × 100`; `ใหม่` is shown when the previous value is zero and the current value is positive.
- Updated A4 landscape print CSS to include the map, comparison table, trend, risk distribution and Top 10 in one page.
