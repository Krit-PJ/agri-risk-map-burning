# HS Range Apply Cleanup

- Removed the visible legacy year dropdown from the UI.
- Kept the hidden internal year selector only for backward compatibility with existing JavaScript state.
- Renamed the range action button from “เดือนล่าสุด” to “แสดงข้อมูล”.
- The button now applies the selected month-year range instead of resetting to the latest month.
- Other dashboard, map, login, print, and sidebar behavior remains unchanged.
